#include <stdio.h>

struct Engine
{
    int bhp;
};

int main()
{
    struct Engine engine;
    scanf("%d",&engine.bhp);
    printHorsePower(&engine);
    return 0;
}

int printHorsePower(struct Engine *ptr)
{
    printf("%d",ptr->bhp);
}
